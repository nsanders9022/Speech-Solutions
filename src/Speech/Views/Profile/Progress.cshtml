@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using System.Security.Claims

@model Speech.Models.Profile


<div class="container">
    @if (User.Identity.IsAuthenticated)
    {

        <h1>View @Html.DisplayFor(model => model.ClientFirst)'s Progress</h1>
        <hr />

        <div class="row">
            <h2>Goals:</h2>
            @if (Model.Goals.Count > 0)
            {
                <div class="row">
                    <div class="col-sm-2">
                        <h4 class="not-met">In progress:</h4>
                    </div>
                    <div class="col-sm-10">
                        @foreach (var goal in Model.Goals)
                        {
                            @if (goal.Completed == false)
                            {
                                <li class="goal-list">@goal.Description</li>
                            }
                        }
                    </div>
                </div>

                <div class="row">
                    <div class="col-sm-2">
                        <h4 class="met">Completed:</h4>
                    </div>
                    <div class="col-sm-10">
                        @foreach (var goal in Model.Goals)
                        {
                            @if (goal.Completed == true)
                            {
                                <li class="goal-list">@goal.Description</li>
                            }
                        }
                    </div>
                </div>
            }
            else
            {
                <p class="goal-list">No goals have been added yet by the therapist.</p>
            }
        </div>

        <hr />

        <div class="row">
          
            <h2>Notes:</h2>
                @if (Model.Notes.Count > 0)
                {
                    <div class="row">
                        @foreach (var note in Model.Notes)
                        {
                            <div class="col-sm-2">
                                <h5>@note.Date</h5>
                            </div>
                            <div class="col-sm-10">
                                <li class="goal-list">@note.Description</li>
                            </div>

                        }
                    </div>
                }
                else
                {
                    <p class="goal-list">No notes have been added yet by the therapist.</p>
                }
        </div>
    }
 </div>