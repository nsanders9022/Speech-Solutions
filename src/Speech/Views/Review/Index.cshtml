@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model IEnumerable<Speech.Models.Review>

<h1>Reviews</h1>

<div class="container">
    @foreach(var product in Model)
    {
        <div class="review">
            <h3>@product.Name</h3>
            <p>@product.Description</p>
        </div>

    }

    @if (User.Identity.IsAuthenticated)
    {
        <button type="submit" class="btn btn-primary">@Html.ActionLink("Add a review", "Create", "Review")</button>
    }

</div>